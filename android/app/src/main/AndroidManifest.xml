<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Phase 11 section 2: only meaningful on API 33+ (Tiramisu) - a
         runtime-requested permission below that has no effect and is
         simply ignored by the platform, so this is safe to declare
         unconditionally given minSdk 26. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required since Android 11 (API 30) package-visibility filtering, so
         SpeechRecognizer.isRecognitionAvailable() can actually see the
         on-device recognition service instead of silently returning false. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>

        <!-- Phase 8 (Application Manager): required for
             PackageManager.queryIntentActivities(ACTION_MAIN/CATEGORY_LAUNCHER)
             to actually enumerate other apps' launcher activities under
             Android 11+ package-visibility filtering - without this,
             AndroidAppManager.installedLaunchableApps() would see almost no
             apps at all (typically just this one). This is the one entry
             in this block that automation genuinely cannot work without;
             the ones below are defensive best-practice for the Intent Tool
             (plain startActivity() with a common implicit action generally
             still works without a matching <queries> entry, but declaring
             them explicitly is what Android's own package-visibility
             guidance recommends and avoids relying on that fallback). -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:name=".AtlasApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Atlas"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Atlas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Phase 8: Android Automation Foundation - Accessibility Service module. -->
        <service
            android:name=".automation.AtlasAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Phase 8: Android Automation Foundation - Notification Listener module. -->
        <service
            android:name=".automation.AtlasNotificationListenerService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Phase 11 section 2: removes WorkManager's default
             auto-initialization (which would use a plain default
             WorkerFactory, unable to construct ProactiveSuggestionsWorker's
             Hilt-injected dependencies) so AtlasApplication's own
             Configuration.Provider - with HiltWorkerFactory - is what
             actually initializes WorkManager instead. Standard, documented
             AndroidX pattern for combining Hilt with WorkManager. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
