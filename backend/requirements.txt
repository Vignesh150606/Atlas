fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.3
pydantic-settings==2.2.1
sqlalchemy==2.0.28
aiosqlite==0.20.0
alembic==1.13.1
python-dotenv==1.0.1
httpx==0.27.0
pytest==8.3.3
pytest-asyncio==0.24.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
structlog==24.1.0
pypdf==4.3.1
python-dateutil==2.9.0
# Phase 12 (ARCH-TZ): stdlib zoneinfo has no bundled tzdata on Windows (and
# on some minimal Linux base images) - it looks for the OS's own IANA
# database and raises ZoneInfoNotFoundError if none is found. This package
# provides that data as a normal importable module so zoneinfo works
# identically across dev (Windows) and deployment (Linux) without relying
# on the host OS having tzdata installed. See app/utils/timezone.py.
tzdata==2024.1
